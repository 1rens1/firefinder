# firefinder

Using `uv`.

Setup venv:

```sh
uv venv
```

Install `requirements.txt` :

```sh
uv pip install -r requirements.txt
```

## Install dataset

```sh
uv run python scripts/download_flame3.py
```
